name: 'mcid'

version: 1.0.0
config-version: 2

profile: mcid

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  mcid:
    +database: cidades
    metadata:
      +materialized: incremental
      +schema: metadata
    # Três schemas para o projeto inteiro (bronze, prata, ouro), um por camada, com a
    # camada no prefixo do nome da tabela. A linha entra no nome a partir da prata
    # (prata_far_*, ouro_far_*); na bronze o nome é o do arquivo que veio da staging,
    # que já identifica a origem.
    # A bronze é dbt: cada model lê o parquet da staging direto por read_parquet
    # (pg_duckdb) e materializa como tabela text, sem script externo.
    far_dbt:
      +materialized: table
      bronze:
        +schema: bronze
      prata:
        +schema: prata
      ouro:
        +schema: ouro

    # Mesma estrutura do far_dbt: bronze/prata/ouro em três schemas do projeto.
    fds_dbt:
      +materialized: table
      bronze:
        +schema: bronze
      prata:
        +schema: prata
      ouro:
        +schema: ouro

    # Um schema só para o domínio, com a camada no prefixo do nome da tabela
    # (bronze_*, silver_*, gold_*). Batendo o olho no banco a listagem sai ordenada
    # por camada, e o Superset aponta para um schema único.
    # A bronze também vive aqui, carregada pelo scripts/staging_para_bronze.py
    # (família `empreendimento_rural` do bronze_familias.yml) — não é dbt.
    empreendimento_rural_dbt:
      +materialized: table
      +schema: empreendimento_rural
      silver:
        +materialized: table
      gold:
        +materialized: table

    conjuntura_dbt:
      +materialized: table
      +schema: conjuntura
      bronze:
        +materialized: table
        +schema: conjuntura_bronze
      silver:
        +materialized: table
        +schema: conjuntura_silver
      gold:
        +materialized: table
        +schema: conjuntura_gold
    
    conjuntura_continuo_dbt:
      +materialized: table
      +schema: conjuntura_continuo
      # Sem camada bronze: o parquet de staging já sai tipado (Etapa 02) e a
      # silver lê direto via pg_duckdb (read_parquet).
      silver:
        +materialized: table
        +schema: conjuntura_continuo_silver
      gold:
        +materialized: table
        +schema: conjuntura_continuo_mart

on-run-start:
  - '{{create_udfs()}}'

# Registra em lake._dbt_log o que não passou — inclusive os testes que são
# `severity: warn` e por isso não derrubam a execução.
on-run-end:
  - '{{ registrar_execucao_dbt() }}'

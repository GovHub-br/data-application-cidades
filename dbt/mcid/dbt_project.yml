name: 'mcid'

version: 1.0.0
config-version: 2

profile: mcid

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  mcid:
    # D3 (pipeline-bronze-historica-destino-trocavel): `cidades` constante nos
    # tres modos. O catalogo local se chama `cidades` (arquivo cidades.duckdb) e
    # no modo C o Postgres e atachado com alias `cidades` — assim o FQN do no no
    # manifest.json e `cidades.<schema>.<tabela>` em qualquer execucao, casando
    # com a tabela real que o OpenMetadata ingere. Substitui o ternario que
    # apontava para o catalogo `mcid_staging` fora do Postgres.
    +database: cidades
    metadata:
      +materialized: incremental
      +schema: metadata
    empreendimento_far_dbt:
      # Convenção 2026-09-08 (consolidar-schemas-historico-reloginho, D1/D6):
      # o que é POR FRENTE (silver_atual_/gold_atual_) fica no schema da frente
      # empreendimento_far. As bronzes desta frente que NÃO são alcançadas pelo
      # fecho transitivo do eixo/gargalo nem carregam sinal preditivo próprio
      # foram removidas — restam só as load-bearing (ver models/docs). As
      # bronzes mantidas continuam em dados_historicos (são insumo do medalhão
      # atual que o gargalo consome). Roda sempre no prod (sem gate de target).
      +materialized: table
      +meta:
        governance:
          product: empreendimento_far
          owner_key: mcid_data_engineering

      bronze:
        +materialized: table
        +schema: dados_historicos
        +meta:
          governance:
            product: empreendimento_far
            owner_key: mcid_data_engineering
            layer: bronze
            classification: restricted
            rag_publication: prohibited
          openmetadata:
            domain: MCid.Habitacao
            tier: Tier.Tier3
            owner: mcid-data-engineering

      silver:
        +materialized: table
        +schema: empreendimento_far
        +meta:
          governance:
            product: empreendimento_far
            owner_key: mcid_data_engineering
            layer: silver
            classification: internal
            rag_publication: eligible_after_security_validation
          openmetadata:
            domain: MCid.Habitacao
            tier: Tier.Tier2
            owner: mcid-data-engineering

      gold:
        +materialized: table
        +schema: empreendimento_far
        +meta:
          governance:
            product: empreendimento_far
            owner_key: mcid_data_engineering
            layer: gold
            classification: internal
            rag_publication: eligible_after_security_validation
          openmetadata:
            domain: MCid.Habitacao
            tier: Tier.Tier1
            owner: mcid-data-engineering

    empreendimento_fds_dbt:
      # Convenção 2026-09-08 — ver empreendimento_far_dbt. Schema silver/gold
      # compartilhado com os colegas é empreendimentos_fds (plural — mesmo
      # nome que eles usam em prod). Bronzes fora do critério de fecho
      # transitivo / sinal preditivo removidas.
      +materialized: table
      +meta:
        governance:
          product: empreendimento_fds
          owner_key: mcid_data_engineering

      bronze:
        +materialized: table
        +schema: dados_historicos
        +meta:
          governance:
            product: empreendimento_fds
            owner_key: mcid_data_engineering
            layer: bronze
            classification: restricted
            rag_publication: prohibited
          openmetadata:
            domain: MCid.Habitacao
            tier: Tier.Tier3
            owner: mcid-data-engineering

      silver:
        +materialized: table
        +schema: empreendimentos_fds
        +meta:
          governance:
            product: empreendimento_fds
            owner_key: mcid_data_engineering
            layer: silver
            classification: internal
            rag_publication: eligible_after_security_validation
          openmetadata:
            domain: MCid.Habitacao
            tier: Tier.Tier2
            owner: mcid-data-engineering

      gold:
        +materialized: table
        +schema: empreendimentos_fds
        +meta:
          governance:
            product: empreendimento_fds
            owner_key: mcid_data_engineering
            layer: gold
            classification: internal
            rag_publication: eligible_after_security_validation
          openmetadata:
            domain: MCid.Habitacao
            tier: Tier.Tier1
            owner: mcid-data-engineering

    # empreendimento_rural_dbt REMOVIDO (change consolidar-schemas-historico-reloginho,
    # D6/D7): a pilha rural "atual" (7 bronzes + 2 silvers) era fork mais pobre
    # do que a pilha dos colegas já em prod (empreendimento_rural, 34 tabelas) e
    # nunca era construída localmente. O rural histórico vem do INT057/INT065 do
    # eixo (mcmv_historico_dbt), não da frente. O braço rural de
    # mcmv_silver_dbt foi repontado para o eixo histórico.

    indicadores_mcmv_dbt:
      # Eixo histórico: `cidades` explícito (herdado da raiz, repetido aqui
      # porque a D3 o exige como invariante deste eixo).
      +database: cidades
      # Convenção 2026-09-08 (consolidar-schemas-historico-reloginho, D1/D5):
      # reloginho é um domínio analítico autocontido — TODAS as camadas
      # (bronze, silver, gold) no schema "reloginho". As bronzes de eventos de
      # entrega saíram de dados_historicos para cá.
      +materialized: table
      +schema: reloginho
      bronze:
        +materialized: table
        +schema: reloginho
      silver:
        +materialized: table
        +schema: reloginho
      gold:
        +materialized: table
        +schema: reloginho

    mcmv_silver_dbt:
      # Convenção 2026-09-04: cada frente (inclusive as sem schema de colega)
      # ganha schema próprio; tabela silver_historico_base em cada uma (via
      # alias, já que o nome do arquivo segue frente-qualificado por exigência
      # de unicidade do dbt). "core" (union de todas as frentes) foi aposentado
      # — quem precisar do consolidado une os schemas na hora.
      +materialized: table
      silver:
        +materialized: table
        far:
          +schema: empreendimento_far
        rural:
          +schema: empreendimento_rural
        entidades:
          +schema: empreendimentos_fds
        conjuntura:
          +schema: conjuntura
        classe_media:
          +schema: classe_media
        reforma:
          +schema: reforma
        cidades:
          +schema: cidades
        sub50:
          +schema: sub50
        pro_moradia:
          +schema: pro_moradia
        minha_casa_minha_vida:
          +schema: minha_casa_minha_vida

    mcmv_historico_dbt:
      # Eixo histórico: `cidades` explícito (herdado da raiz, repetido aqui
      # porque a D3 o exige como invariante deste eixo).
      +database: cidades
      # Convenção 2026-09-08 (consolidar-schemas-historico-reloginho, D1):
      # `dados_historicos` é o schema do DOMÍNIO histórico cross-frente, em
      # TODAS as camadas — bronze, silver (incl. serie_executiva e entrega_apf)
      # e gold (os 4 cross-frente). O que é POR FRENTE (os 3
      # silver_historico_empreendimento far/fds/rural) usa alias para
      # materializar no schema da frente. O piloto FGTS/OGU (agregado macro
      # anual, não é frente) usa conjuntura. Os schemas mcmv_historico e
      # serie_historica foram extintos.
      +materialized: table
      +meta:
        governance:
          product: mcmv_historico
          owner_key: mcid_data_engineering

      bronze:
        +materialized: table
        +schema: dados_historicos
        +meta:
          governance:
            product: mcmv_historico
            owner_key: mcid_data_engineering
            layer: bronze
            classification: restricted
            rag_publication: prohibited
          openmetadata:
            domain: MCid.Habitacao
            tier: Tier.Tier3
            owner: mcid-data-engineering

      silver:
        +materialized: table
        +schema: dados_historicos
        +meta:
          governance:
            product: mcmv_historico
            owner_key: mcid_data_engineering
            layer: silver
            classification: internal
            rag_publication: eligible_after_security_validation
          openmetadata:
            domain: MCid.Habitacao
            tier: Tier.Tier2
            owner: mcid-data-engineering

      gold:
        +materialized: table
        +schema: dados_historicos
        +meta:
          governance:
            product: mcmv_historico
            owner_key: mcid_data_engineering
            layer: gold
            classification: internal
            rag_publication: eligible_after_security_validation
          openmetadata:
            domain: MCid.Habitacao
            tier: Tier.Tier1
            owner: mcid-data-engineering

      # Piloto #118: série anual OGU/FGTS a partir de seed — agregado macro
      # nacional, não é frente de empreendimento; materializa em conjuntura.
      piloto:
        +materialized: table
        +schema: conjuntura
        +meta:
          governance:
            product: mcmv_historico
            owner_key: mcid_data_engineering
            layer: silver
            classification: internal
            rag_publication: eligible_after_security_validation
          openmetadata:
            domain: MCid.Habitacao
            tier: Tier.Tier2
            owner: mcid-data-engineering

    entidades_dbt:
      +materialized: table
      +schema: entidades_fds
      silver:
        +materialized: table
      gold:
        +materialized: table

    conjuntura_dbt:
      +materialized: table
      +schema: conjuntura
      bronze:
        +materialized: table
        +schema: conjuntura_bronze
      silver:
        +materialized: table
        +schema: conjuntura_silver
      gold:
        +materialized: table
        +schema: conjuntura_gold

    conjuntura_continuo_dbt:
      +materialized: table
      +schema: conjuntura_continuo
      # Sem camada bronze: o parquet de staging já sai tipado (Etapa 02) e a
      # silver lê direto via pg_duckdb (read_parquet).
      silver:
        +materialized: table
        +schema: conjuntura_continuo_silver
      gold:
        +materialized: table
        +schema: conjuntura_continuo_mart

on-run-start:
  - "{% if target.type == 'postgres' %}{{ create_udfs() }}{% endif %}"

seeds:
  mcid:
    # Piloto #118 (série anual OGU/FGTS): agregado macro nacional, não é frente.
    # Consolidado em `conjuntura` junto do consumidor
    # silver_mcmv_historico_serie_anual_ogu_fgts (change
    # consolidar-schemas-historico-reloginho, D1). CSV em seeds/conjuntura/.
    conjuntura:
      +schema: conjuntura
      +quote_columns: false
    entidades_fds:
      +schema: entidades_fds
      +quote_columns: false
    # Seeds de referência dos testes de qualidade (change
    # testes-data-quality-dbt): domínios canônicos e retrato de colunas por
    # família. `cidades` explícito porque o eixo histórico exige o catálogo
    # `cidades` como invariante (D3 pipeline-bronze-historica-destino-trocavel).
    data_quality:
      +database: cidades
      +schema: data_quality
      +quote_columns: false
      # chave_natural / faixa são texto — várias chaves são só dígitos e a
      # inferência as tornaria BIGINT, quebrando o join com a silver (varchar).
      # Change: vocabulario-e-qualidade-financeira-historica.
      quarentena_valores_financeiros:
        +column_types:
          fonte_familia: varchar
          chave_natural: varchar
          motivo: varchar
          evidencia: varchar
      faixa_valor_uh:
        +column_types:
          frente: varchar
          faixa: varchar
          valor_col: varchar
          piso: double
          teto: double
